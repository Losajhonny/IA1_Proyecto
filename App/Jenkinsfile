pipeline {
    agent any
    tools { nodejs "node16.2.0" }
    stages {
        stage ("install") {
            steps { sh "npm install" }
        }
        stage ("test") {
            steps { sh "npm run test --watch=false" }
        }
        stage ("deploy") {
            steps { sh "npm run start" }
        }
    }
}
