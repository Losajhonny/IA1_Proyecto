pipeline {
    agent {
        docker {
            image "node"
            args "-p 3000:3000"
        }
    }
    /*tools { nodejs "node" }*/
    stages {
        stage ("install") {
            steps { sh "npm install" }
        }
        stage ("test") {
            steps { sh ".npm test" }
        }
        stage ("deploy") {
            steps { sh "npm start" }
        }
    }
}
