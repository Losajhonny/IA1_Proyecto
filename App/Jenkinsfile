pipeline {
    agent { label "master" }
    tools { nodejs "node16.2.0" }
    stages {
        stage ("install") {
            steps { sh "npm install" }
        }
        stage ("test") {
            steps { sh "ng test" }
        }
        stage ("deploy") {
            steps { sh "hola" }
        }
    }
}
