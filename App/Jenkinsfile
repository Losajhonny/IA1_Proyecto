pipeline {
    agent any
    tools { nodejs "node16.2.0" }
    stages {
        stage ("build") {
            steps {
                sh "npm install"
            }
        }
        stage ("test") {
            steps {
                sh "ng test"
            }
        }
    }
}
